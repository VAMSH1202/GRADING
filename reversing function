#include<stdio.h> 
#include<string.h> 
#include<stdlib.h> 
int main(){ 
 
    char str[100]; 
    int i; 
    printf("Enter your string: "); 
    fgets(str, sizeof str, stdin); 
 
    for(i=strlen(str);i>=0;i--)
    {
        printf("%c ",str[i]);  
    } 
} 
